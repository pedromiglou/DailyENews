apiVersion: v1
kind: Service
metadata:
  name: redis-primary-cluster-service
  namespace: gic2
spec:
  type: LoadBalancer
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: redis-primary
